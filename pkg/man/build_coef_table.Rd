% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{build_coef_table}
\alias{build_coef_table}
\title{Build Coefficient Table from glmnet Model}
\usage{
build_coef_table(model, lambda = NULL, gamma = NULL)
}
\arguments{
\item{model}{A fitted \code{glmnet} or \code{cv.glmnet} object.}

\item{lambda}{Numeric lambda value at which to extract coefficients.
If \code{NULL} and model is a cv.glmnet object, uses \code{lambda.1se}.}

\item{gamma}{Numeric gamma value for relaxed fits (0 = fully relaxed,
1 = regular glmnet). Only used for relaxed glmnet models. Default
\code{NULL} (ignored for non-relaxed fits).}
}
\value{
A data frame with columns \code{variable} and \code{coefficient}.
}
\description{
Extracts coefficients from a fitted glmnet or cv.glmnet model at a
given lambda value and returns a tidy data frame.
}
\examples{
x <- matrix(rnorm(100 * 5), 100, 5,
            dimnames = list(NULL, paste0("V", 1:5)))
y <- rnorm(100)
fit <- glmnet::cv.glmnet(x, y)
build_coef_table(fit)
}
