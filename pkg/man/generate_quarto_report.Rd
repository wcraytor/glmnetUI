% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_report.R
\name{generate_quarto_report}
\alias{generate_quarto_report}
\title{Generate a Quarto report bundle (without rendering)}
\usage{
generate_quarto_report(assets_args, dest_dir, base = "glmnet_report")
}
\arguments{
\item{assets_args}{Named list of arguments forwarded to
\code{\link[=prepare_report_assets]{prepare_report_assets()}} (\code{model}, \code{lambda}, \code{gamma}, \code{x_mat}, \code{y_vec},
\code{coef_df}, \code{predictors}, \code{response}, \code{data}, and the optional extras).
\code{assets_dir} is supplied by this function and must not be included.}

\item{dest_dir}{Directory to write the bundle into (typically the project's
\verb{<os>_out_glmnet} folder).}

\item{base}{Bundle base name (no extension). Defaults to \code{"glmnet_report"}.}
}
\value{
Invisibly, the absolute path to the generated \code{.qmd} file.
}
\description{
Writes a self-contained Quarto bundle under \verb{<dest_dir>/<base>_qmd/}
containing the populated \verb{<base>.qmd}, all pre-generated plots, the report
data RDS (\code{report_data.rds}), and \code{reference.docx}. Render it to HTML /
Word / PDF with \code{\link[=convert_quarto_file]{convert_quarto_file()}}.
}
